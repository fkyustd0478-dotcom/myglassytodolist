'use strict';

// Fill this with the Firebase Web App config from Firebase Console.
// Authentication only: do not add Firestore, database, or storage config here.
window.LAPIS_FIREBASE_CONFIG = window.LAPIS_FIREBASE_CONFIG || null;
